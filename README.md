# 🩺 Skin Analyzer

Skin Analyzer is a Deep Learning-based Skin Disease Classification system that uses a Convolutional Neural Network (CNN) to identify skin diseases from uploaded images. The model is trained on dermatology image datasets and deployed as a Flask web application for real-time predictions.

## 🚀 Features

* Detects multiple skin disease categories from uploaded images
* CNN-based image classification using TensorFlow/Keras
* Real-time prediction with confidence score display
* Interactive Flask web interface
* Image upload and preview functionality
* Lightweight and easy to run locally
* Displays a medical disclaimer for responsible usage

## 🏥 Supported Skin Diseases

The model can classify the following skin conditions:

* Psoriasis
* Rosacea
* Seborrheic Keratoses
* Skin Cancer
* Sunlight Damage
* Tinea
* Vascular Tumors
* Vasculitis
* Vitiligo
* Warts

## 🛠️ Technologies Used

### Backend

* Python
* Flask
* TensorFlow / Keras
* OpenCV
* NumPy
* Scikit-learn

### Frontend

* HTML
* CSS
* Jinja2

## 🧠 Model Architecture

```text
Input Image (128x128x3)
        ↓
Conv2D (32 Filters) + ReLU
        ↓
MaxPooling2D
        ↓
Conv2D (64 Filters) + ReLU
        ↓
MaxPooling2D
        ↓
Conv2D (128 Filters) + ReLU
        ↓
MaxPooling2D
        ↓
Flatten
        ↓
Dense (128) + ReLU
        ↓
Dropout (0.5)
        ↓
Softmax Output Layer
```

## 📂 Project Structure

```text
Skin-Analyzer/
│
├── dataset/
│   ├── train/
│   └── test/
│
├── model/
│   ├── skin_model.h5
│   └── classes.npy
│
├── static/
│   └── uploads/
│
├── templates/
│   ├── index.html
│   └── result.html
│
├── train.py
├── app.py
├── requirements.txt
└── README.md
```

## ⚙️ Installation

### Clone the Repository

```bash
git clone https://github.com/your-username/Skin-Analyzer.git
cd Skin-Analyzer
```

### Create a Virtual Environment

```bash
python -m venv venv
```

Activate the environment:

**Windows**

```bash
venv\Scripts\activate
```

**Linux / macOS**

```bash
source venv/bin/activate
```

### Install Dependencies

```bash
pip install -r requirements.txt
```

## 🏋️ Train the Model

Place your dataset in the following structure:

```text
dataset/
├── train/
│   ├── Psoriasis/
│   ├── Rosacea/
│   └── ...
└── test/
    ├── Psoriasis/
    ├── Rosacea/
    └── ...
```

Run:

```bash
python train.py
```

The trained model will be saved in:

```text
model/skin_model.h5
model/classes.npy
```

## ▶️ Run the Application

Start the Flask server:

```bash
python app.py
```

Open your browser and visit:

```text
http://127.0.0.1:5000
```

Upload a skin image and receive an AI-powered prediction with a confidence score.

## 📊 Performance

| Metric    | Value |
| --------- | ----- |
| Accuracy  | ~85%  |
| Precision | 0.84  |
| Recall    | 0.83  |
| F1-Score  | 0.83  |

> Results may vary depending on dataset size, image quality, and training configuration.

## 🔮 Future Enhancements

* Transfer Learning using ResNet50, EfficientNet, or MobileNet
* Cloud Deployment (AWS, Azure, Render)
* Mobile Application Support
* Explainable AI with Grad-CAM Visualizations
* Larger and More Diverse Medical Datasets
* Multi-image Analysis

 📸screenshots

<img width="1365" height="718" alt="Screenshot 2026-06-07 165534" src="https://github.com/user-attachments/assets/8ea58fd5-d8c7-411f-ad2f-aee362a4853a" />
<img width="1365" height="720" alt="Screenshot 2026-06-07 165706" src="https://github.com/user-attachments/assets/6e871316-6580-41c3-85a3-01358febd5e3" />

## ⚠️ Disclaimer

This project is intended for educational and research purposes only. The predictions generated by the model should not be considered medical diagnoses. Always consult a qualified healthcare professional for proper medical advice and treatment.

## 👨‍💻 Author

**Renganathan V**

Skin Analyzer Project

⭐ If you found this project useful, consider giving it a star on GitHub.
